<form bindsubmit="formSubmit" bindreset="formReset">
  <!-- <view class="section">
    <input type="text" name="asset_uuid" bindinput='input_uuid' value="{{asset_uuid}}" placeholder="物品编号" />
    <view catchtap='click_scan' class='scanBtn' ><image style='width:70rpx;height:70rpx;' src='/images/s.png' ></image></view>
  </view>
  <view class="section">
    <input type="text" name="name" value="{{asset_name}}" placeholder="物品名称" />
  </view>
  <view class="section">
    <input type="text" name="category_id" value="{{category}}" placeholder='物品类别' />
  </view> -->

  <view class="section">
  <view class="section__title">普通选择器：</view>
  <picker bindchange="bindPickerChange" value="{{index}}" range="{{fields1}}">
    <view class="picker">
      {{fields1[index]['1']}}
    </view>
  </picker>
</view>

  <view class="section">
    <input type="text" name="field_id" value="{{field}}" placeholder='所在场地' />
  </view>
  <view class="textarea_view">
    <textarea name="remarks" placeholder='问题描述' ></textarea>
  </view>
  <view class='section' style='border-bottom:none;' >
    <block wx:for="{{imgs}}">
      <image src='{{item}}' class='uploader_imgs'  data-src="{{item}}" bindtap='imgShow'></image>
    </block>
    <view class='uploaderBtn' bindtap='selectImg'>
      <image src='{{uploaderImg}}'></image>
    </view>
    <text class='prompt'>拍摄照片帮助\n我们更快解决问题</text>
  </view>
  <view class="btn-area">
     <!-- <button class='submitBtn' disabled='{{disabled}}' formType="submit">提交</button> -->
     <button class='submitBtn' formType="submit">提交</button>
  </view>
</form>